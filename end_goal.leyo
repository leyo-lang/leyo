~ End goal showcase program for Leyo ~

int base = 8;
int height = 12;
int area = base * height;
int halfArea = area / 2;
int squared = halfArea ^ 2;
int total = squared + area - base;

total = total + 1;